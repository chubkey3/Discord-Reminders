const a = require('./commands/a')
const as = require('./commands/as')
const am = require('./commands/am')
const d = require('./commands/d')
const l = require('./commands/l')
const h = require('./commands/h')
const tz = require('./commands/tz')

module.exports = {
    a,
    as,
    am,
    d,
    l,
    h,
    tz
}